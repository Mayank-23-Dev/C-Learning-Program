#include <iostream>
using namespace std;
int main()
{
    int check=0,i;
    string s1, s2;
    cout << "Enter First String-: ";
    getline(cin, s1);
    cout << "Enter Second String-: ";
    getline(cin, s2);
    int len1 = s1.length();
    for ( i = 0; i < len1; i++)
    {
        if (s1[i]= s2[0]) 
        {
            
        } 
    }
}